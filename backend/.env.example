# Immigrow Backend Environment Variables
# Copy this file to .env and fill in your values

# ==============================================================================
# DATABASE CONFIGURATION (PostgreSQL Recommended)
# ==============================================================================

# RECOMMENDED FOR DEVELOPMENT: PostgreSQL with individual variables
# Uncomment and fill in these values:

POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_password_here
POSTGRES_DATABASE=immigrow

# ALTERNATIVE FOR PRODUCTION: Single DATABASE_URL (used by Heroku, AWS, etc.)
# If DATABASE_URL is set, it will override the individual variables above
# DATABASE_URL=postgresql://user:password@host:port/database

# ALTERNATIVE: MySQL (if you prefer MySQL over PostgreSQL)
# MYSQL_HOST=localhost
# MYSQL_PORT=3306
# MYSQL_USER=root
# MYSQL_PASSWORD=your_password_here
# MYSQL_DATABASE=immigrow

# FALLBACK: SQLite (automatic if no database config is set)
# No configuration needed - just delete/comment out all database variables above

# ==============================================================================
# API KEYS
# ==============================================================================

# Eventbrite API (REQUIRED for fetching events)
# Get your token at: https://www.eventbrite.com/platform/api#/introduction/authentication
EVENTBRITE_API_TOKEN=your_eventbrite_token_here

# CourtListener API (OPTIONAL - increases rate limits)
# Get your token at: https://www.courtlistener.com/help/api/rest/
COURTLISTENER_API_TOKEN=your_courtlistener_token_here

# ==============================================================================
# FLASK CONFIGURATION
# ==============================================================================

# Flask environment (development/production)
FLASK_ENV=development

# Flask app port
PORT=5000

# ==============================================================================
# FRONTEND CONFIGURATION (from root .env.example)
# ==============================================================================

# AWS Configuration
AWS_DEFAULT_REGION=us-east-1
S3_BUCKET=immigrow.site
CF_DIST_ID=E3VBUSY2EV16W

# GitLab Configuration
VITE_GITLAB_BASE=https://gitlab.com/api/v4
VITE_GITLAB_PROJECT_PATH=anisha1045/cs373-55090-09
